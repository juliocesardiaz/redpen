# Comment bank

## Missing docstring

Add a docstring explaining what the function does.

Use `"""triple quotes"""` right under the `def` line.

## Magic numbers

This value appears without explanation:

```python
# example — a bare constant
x = 42

y = x * 2
```

Name it as a constant instead.

## Great variable names

Your variable names make this easy to read — keep doing that.
